import { MyAttrDirDirective } from './my-attr-dir.directive';

describe('MyAttrDirDirective', () => {
  it('should create an instance', () => {
    const directive = new MyAttrDirDirective();
    expect(directive).toBeTruthy();
  });
});
